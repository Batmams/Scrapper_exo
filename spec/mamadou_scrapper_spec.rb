require_relative '../lib/mamadou_scrapper'

describe "for crypto_name" do
    it "should show crypto name" do
      expect(crypto_name).to be_an_instance_of(Array)
    end
  end

  describe "for price" do
    it "should show crypto value" do
        expect(crypto_name).to be_an_instance_of(Array)
    end
end






